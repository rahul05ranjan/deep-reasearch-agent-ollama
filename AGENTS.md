# AGENTS.md

This file contains instructions and configuration for AI agents working in this repository.

## Agent skills

### Issue tracker

GitHub Issues via `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default 5-role canonical triage labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` and `docs/adr/` at repo root). See `docs/agents/domain.md`.

### Coding standards

TypeScript conventions, Zod runtime contracts, and test fixtures. See `CODING_STANDARDS.md`.
